import React from "react";

const ProductCard = ({ productData }) => {
    const {
        title,
        description,
        price,
        discountPercetage,
        rating,
        brand,
        category,
        thumbnail
    } = productData;

    return <div></div>;
}
export default ProductCard;